
fn fib_sqaure_sum: x, y => {
	fn square: x => return x ** 2
	
	fn fib: n => {
		if: n <= 2 => return 1

		return fib(n - 1) + fib(n - 2)
	}

	return square(fib(x)) + square(fib(y))
}

obj = {
	x: 1,
	y: 2,
	complex: {
		a: 0,
		foo,
		shamen
	},
	z: max,
}